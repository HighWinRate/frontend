# Copy this file to .env.local and update the values with your Supabase project details.
# cp env.example.txt .env.local

# Supabase (public client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase (server-only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Landing Page URL (used in Navbar links)
NEXT_PUBLIC_LANDING_URL=http://localhost:3003

# Optional: override the app name shown in the UI
NEXT_PUBLIC_APP_NAME=High Win Rate
