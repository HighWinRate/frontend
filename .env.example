# ============================================
# Web App Environment Variables
# ============================================
# این فایل template است. برای استفاده:
# 1. این فایل را به .env.local کپی کنید
# 2. مقادیر را از .env global (در root) یا مقادیر واقعی جایگزین کنید

# ============================================
# API Configuration
# ============================================
# URL Backend API
NEXT_PUBLIC_API_URL=${BACKEND_URL:-http://localhost:3000}

# ============================================
# Web App URLs
# ============================================
# URL Web App (برای redirects)
NEXT_PUBLIC_WEB_APP_URL=${WEB_APP_URL:-http://localhost:3001}

# URL Landing Page (برای redirects)
NEXT_PUBLIC_LANDING_URL=${LANDING_URL:-http://localhost:3003}
